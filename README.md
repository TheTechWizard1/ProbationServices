# Система Службы Пробации (Probation Services)

## Обзор проекта

**Probation Services** — это веб-приложение на Django, предназначенное для автоматизации процессов службы пробации. Оно включает в себя систему заявок (тикетов) для решения проблем и базу знаний в виде инструкций.

Система разделяет пользователей на три роли:
- **Клиент**: Может создавать заявки, просматривать свои заявки и инструкции.
- **Специалист**: Может просматривать все заявки, назначать их себе, изменять статус, а также создавать и редактировать инструкции.
- **Администратор**: Имеет полный доступ ко всем функциям, включая управление пользователями и просмотр общей статистики.

## 🚀 Основные возможности

- **Аутентификация и роли**: Вход в систему по логину/паролю, автоматическое создание профиля, разграничение прав доступа.
- **Система заявок (тикетов)**:
  - Создание, просмотр, фильтрация и пагинация заявок.
  - Назначение заявок специалистам.
  - Изменение статуса (`Новая`, `В работе`, `Решена`, `Закрыта`).
  - Установка приоритета (`Низкий`, `Средний`, `Высокий`, `Критичный`).
- **База знаний**:
  - Создание, редактирование и просмотр инструкций.
  - Категоризация инструкций (`Оборудование`, `ПО`, `Сеть` и др.).
  - Счетчик просмотров.
- **Панель управления (Dashboard)**:
  - Для специалистов и администраторов.
  - Статистика по заявкам, пользователям и инструкциям.
  - Быстрый доступ к последним активностям.
- **REST API**:
  - Эндпоинты для управления заявками и инструкциями.
  - Регистрация пользователей.
  - Аутентификация по JWT-токенам.
- **Экспорт**:
  - Выгрузка списка заявок в формате `.csv`.

## 🛠️ Технологии

- **Бэкенд**: Django, Django REST Framework
- **База данных**: SQLite 3 (по умолчанию)
- **Аутентификация**: Django REST Framework Simple JWT
- **Фронтенд**: HTML, CSS, JavaScript (с использованием шаблонизатора Django)

## ⚙️ Установка и запуск

1. **Клонируйте репозиторий**:
   ```bash
   git clone <URL вашего репозитория>
   cd ProbationServices
   ```

2. **Создайте и активируйте виртуальное окружение**:
   ```bash
   python -m venv venv
   source venv/bin/activate  # Для Windows: venv\Scripts\activate
   ```

3. **Установите зависимости**:
   ```bash
   pip install -r requirements.txt
   ```
   *Примечание: Файл `requirements.txt` нужно создать. Основные зависимости: `django`, `djangorestframework`, `djangorestframework-simplejwt`.*

4. **Примените миграции**:
   ```bash
   python manage.py migrate
   ```

5. **Создайте суперпользователя**:
   ```bash
   python manage.py createsuperuser
   ```

6. **Запустите сервер для разработки**:
   ```bash
   python manage.py runserver
   ```

Приложение будет доступно по адресу `http://127.0.0.1:8000/`.

## 📂 Структура проекта

```
pythonProject1/
├── api/                # Основное приложение
│   ├── migrations/     # Миграции базы данных
│   ├── templates/      # HTML-шаблоны для приложения api
│   ├── admin.py        # Настройки админ-панели
│   ├── apps.py         # Конфигурация приложения
│   ├── models.py       # Модели данных (Ticket, Instruction, UserProfile)
│   ├── serializers.py  # Сериализаторы для API
│   ├── urls.py         # Маршруты приложения api
│   └── views.py        # Представления (views)
├── backend/            # Основная конфигурация проекта Django
│   ├── settings.py     # Глобальные настройки
│   ├── urls.py         # Корневые маршруты
│   └── wsgi.py
├── static/             # Статические файлы (CSS, JS, изображения)
├── templates/          # Общие шаблоны
├── manage.py           # Утилита для управления проектом
└── db.sqlite3          # Файл базы данных
```

## 🔌 Основные API эндпоинты

- **Регистрация**: `POST /api/register/`
- **Получение JWT токена**: `POST /api/token/`
- **Обновление токена**: `POST /api/token/refresh/`

**Инструкции**:
- `GET /api/instructions/` - список всех инструкций
- `GET /api/instructions/{id}/` - получить инструкцию по ID
- `GET /api/instructions/category/{category_name}/` - инструкции по категории

**Заявки (тикеты)**:
- `GET /api/tickets/` - список всех заявок (для специалистов/админов)
- `GET /api/my-tickets/` - список заявок текущего пользователя
- `POST /api/tickets/` - создание новой заявки
- `GET /api/tickets/{id}/` - получить заявку по ID
- `POST /api/tickets/{id}/assign/` - назначить заявку на себя
- `POST /api/tickets/{id}/resolve/` - установить статус "Решена"

Полная документация доступна по эндпоинту `/api/docs/` после запуска сервера.
